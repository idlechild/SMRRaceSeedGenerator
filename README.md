# SMRRaceSeedGenerator

docker build --tag smr . \
RunBoth or RunDiscord or RunRacetime (if having trouble debugging, try removing -d)

docker stop <container_name> \
docker rm <container_name>

docker system prune -a \
docker system df

